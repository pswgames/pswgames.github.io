# 서우놀이터 6.0 — 무료 범위 품질 개선

기준 main: `b4c78f53db17cb431502f006f63bd3a76323df89`.
기존 콘텐츠, `seowoo-play-v3` 저장 키, 점수·스티커·설정을 유지한다.

## 음성

고정 음원 우선 + 기기 TTS fallback(B)을 선택했다. 정적 Pages에서 외부 서버와 프런트엔드 API 키 없이 배포할 수 있다. A(모든 발화 녹음)는 동적 문장까지 녹음하는 비용과 관리 부담이 있고, C(기기 TTS만 사용)는 기기마다 음질이 달라 최종 품질 목표를 보장하지 못한다.

이번에는 유료 서비스를 사용하지 않는다는 사용자 결정에 따라 **실제 Neural TTS 음원은 추가하지 않았다.** `audio/catalog.js`의 554개 항목은 음원 파일이 아닌 대본과 식별자다. `src`가 없는 항목은 기기 TTS를 사용한다. 자연스러운 사람 음성 완료 기준은 미충족이다.

`AudioManager`는 `playVoice`, `playSfx`, `playMusic`, `stopVoice`, `stopAll`, `setVoiceVolume`, `setSfxVolume`, `preload`를 제공한다. 최신 터치가 이전 음성을 취소하며 취소된 Promise도 종료한다. 파일 오류의 중복 fallback을 막는다. 음성·효과음 볼륨을 따로 저장한다. 문제 정답 안내가 끝난 뒤 다음 문제로 진행한다. 영어는 영어 음성을 고른다.

효과음은 클릭·문 마찰·모터를 필터링된 잡음으로, 도착음을 부드러운 두 음으로 합성한다. **실제 환경 녹음이 아니다.** 향후 사용 허가된 녹음으로 교체할 수 있다. 기존 음악 메뉴의 음악 파일은 원래부터 모두 null이며 이번에도 그대로다.

음원을 추가할 때는 같은 항목에 로컬 `src`를 설정하고, 오프라인 필수 음원을 `sw.js` CORE에 함께 추가한 후 파일 누락과 실제 청취를 검사한다. 음원은 일관된 화자·속도·음량으로 제작하고 배포 권리를 확인해야 한다.

## 엘리베이터와 디자인

- 하나의 requestAnimationFrame 시계로 문·층수·배경을 동기화한다. 문 닫힘 전 350ms 대기, 1450ms 문 이동, 닫힘 후 180ms 정지, 층당 1750ms 이동에 600ms 가감속 구간을 적용한다. 도착음 후 500ms 뒤 문을 연다. 이 수치는 앱 안에서 조정한 설계값이며 실제 영상에서 측정한 값은 아니다.
- 문 프레임·반사·손잡이와 가까운 승강로 구조물을 분리해 도시 배경과 다른 속도로 움직인다. 레이아웃 측정은 resize에서만 수행한다. 매 프레임 버튼 20개를 다시 갱신하지 않는다.
- 세로 조작반은 5열, 가로는 10열 두 줄이다. 48px 이상 버튼, 현재층/목적층/예약층 상태, 명시적 전체 화면 버튼을 제공한다.
- 새 도시 배경은 내장 이미지 생성 도구로 제작했다. 원본 1254×1254에서 WebP 299,064 bytes, AVIF 227,943 bytes를 비교했다. 기존 브라우저 호환성과 현재 자산 형식의 일관성을 위해 WebP를 선택했다. AVIF의 작은 파일 크기만으로 디코딩 성능 우위를 주장하지 않는다.
- 색·그림자·모서리·터치 반응·태블릿 그리드를 정리하고 홈 핵심 아이콘을 자체 SVG로 통일했다. 장식용 무한 흔들림을 줄이고 reduced-motion을 지원한다.

## PWA와 회귀 방지

중복 설치 처리와 서비스 워커 등록을 제거했다. 실행 중 강제 reload를 하지 않는다. 필수 자산 중 하나라도 없으면 새 워커 설치를 실패시켜 이전 워커를 유지한다. 업데이트는 모든 기존 창을 닫은 뒤 활성화된다. 백그라운드로만 보내면 새 버전이 아직 대기할 수 있다.

현재 route와 엘리베이터 층을 sessionStorage에 보존한다. 이동 중 백그라운드에서는 멈추고 복귀하면 같은 이동을 이어간다. 앱 프로세스가 완전히 종료되면 마지막 저장 층에서 정지 상태로 시작한다. 놀이별 문제 진행 전체를 직렬화하지는 않는다.

게임 지연 작업을 화면 수명에 묶고, 정답 연타의 중복 집계와 화장실 물내리기 중복 실행을 막았다. 휴식 완료 버튼은 화면을 새로고침하지 않는다. 참조가 없는 임시 파일 8개와 비활성 스타일 2개를 제거했다. 이전 클라이언트 호환을 위한 구 PWA 파일과 이미지들은 보존했다.

## QA

Chrome 자동화: 360/390/430/768/800/1024/1280px 및 800×360, 각 21개 route. 가로 넘침 없음, 모든 층 버튼 48px 이상·화면 안 표시. 문 닫힘 전 이동 금지, 중간 위치, 예약층, 하강, 연타, 화면 이탈 지연 작업 취소, 설정 저장, route 복원, 오프라인 숫자판·엘리베이터를 검사했다. 페이지 예외와 실패 응답 없음.

별도 검사: 음성 취소 완료·preload 재사용·실패 fallback 1회·음소거·볼륨 범위, 업데이트 대기 중 화면 유지·다음 실행 활성화·구 캐시 정리·오프라인 실행·필수 파일 실패 시 이전 버전 유지.

`npm install` 후 설치된 Chrome으로 `npm test`. 자동화 스크린샷은 `qa-artifacts/`에 저장된다. 정적 앱 자체는 빌드나 런타임 의존성이 없다.

물리 Android 태블릿, Android 설치 아이콘의 Chrome 배지 유무, 실제 standalone OS 실행, 저사양 기기의 60fps, 사람 음성 청취 품질은 이 환경에서 검증하지 않았다. 데스크톱 Chrome 화면 크기 모의 검사는 이를 대신하지 못한다.

## 참고 자료

- [Sago Mini World](https://sagomini.com/world/): 아이가 자기 속도로 탐색하는 열린 놀이를 참고했다.
- [Khan Academy Kids](https://www.khanacademy.org/kids): 학습과 창의적 놀이·사회정서 경험을 함께 제공하는 방향을 참고했다. 두 앱의 실제 음성·모션 시간값을 측정하거나 복제하지 않았다.
- [Chrome 서비스 워커 수명주기](https://developer.chrome.com/docs/workbox/service-worker-lifecycle): 업데이트 대기와 기존 클라이언트 유지 원칙.

이미지 생성 프롬프트: “High clarity sunlit modern Korean neighborhood city panorama, cream and pale blue buildings, leafy park at bottom, soft blue sky, straight verticals, warm morning light, usable vertical crops; no elevator, frame, UI, text, signs or watermarks.” 내장 이미지 생성 도구 사용. 프로젝트 파일: `assets/elevator-city-v6.webp`.
