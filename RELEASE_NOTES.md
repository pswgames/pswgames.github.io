# 서우 놀이터 v5.12.0

2026-09-09 · 기준 커밋 f940fedae555eac5afbdf25227829681e8f65f4f

## 반영 내용

- 저장소에 있던 도시 파노라마 타일 5개를 그대로 사용한다. 이미지 파일 자체는 교체하거나 다시 생성하지 않았다.
- 층 이동은 일정 속도 구간에서 층당 1.75초, 출발·도착 가감속은 각각 0.25초다. 한 층만 이동할 때 주행시간은 2초다. 문 닫힘 1.5초는 주행시간과 별도다.
- 상승하면 바깥 풍경이 아래로, 하강하면 위로 이동한다. 층 표시와 배경은 동일한 실제 계산 위치를 사용한다. 배경만 먼저 움직였다가 되돌아오는 보정은 제거했다.
- 높은 층고 느낌을 위해 화면 방향에 맞춰 파노라마의 표시 배율과 세로 이동 범위를 늘렸다. 층 숫자가 빨라지는 방식으로 배경 속도를 올리지 않는다.
- 태블릿 세로 화면은 배경 아래에 버튼, 가로 화면은 배경 오른쪽에 버튼을 배치한다. 테스트 화면에서 20개 층 버튼은 스크롤 없이 표시되고 터치 영역은 최소 44px이다.
- 이동 중 마지막으로 누른 층을 다음 목적지로 표시한다. 도착 후 문 열림과 대기를 마친 뒤 다음 이동을 시작한다.
- 화면을 벗어나면 엘리베이터의 모든 예약 동작을 취소한다. 다시 들어오면 현재 층과 안내문이 일치한다. 화면이 숨겨져 있는 동안 이동 시간을 멈춰 복귀 시 층을 건너뛰지 않게 했다.
- 구형 엘리베이터 타이머를 앱 코드에서 제거했다. 엘리베이터 CSS를 한 파일로 모으고 중복 모션 CSS 로딩을 제거했다.
- HTML, JavaScript, CSS, 파노라마 요청 URL 및 PWA 캐시 버전을 5.12.0으로 맞췄다. 서비스워커 등록은 고정 URL과 updateViaCache:none을 사용한다. 놀이 기록 저장 코드와 localStorage 키는 변경하지 않았다.

## 검증 결과

브라우저 검증은 실제 수정된 소스와 기존 이미지 자산을 Chromium에 로컬 인라인 로딩하여 수행했다. 운영 사이트를 실시간 브라우저로 열어 수행한 검증과는 구분한다.

| 검증 | 결과 |
| --- | --- |
| 768×1024, 820×1180, 1024×768, 1180×820 | 층 버튼 잘림·가로 넘침 없음 |
| 390×844, 375×667, 320×568, 667×375, 844×390, 1280×800 | 층 버튼 잘림·가로 넘침 없음 |
| 1→20층·20→1층 자동 주행 | 모든 층 순서, 배경 방향, 도착 위치 및 배경 빈틈 검사 통과 |
| 층 표시 간격 | 테스트 시계 기준 중간층 약 1.744~1.760초, 출발·도착 인접 구간 약 1.872~1.888초 |
| 한 층 주행 | 테스트 시계 기준 2.000초, 문 닫힘 시간 별도 |
| 연속 입력·다음 목적지·화면 이탈·재진입 | 중복 주행 없이 통과 |
| 주행 중 태블릿 화면 회전 | 위치 초기화 없이 통과 |
| 8초 숨김 후 복귀 | 숨김 동안 위치 유지, 복귀 후 연속 주행 통과 |
| 기존 기능 | 숫자 100개, 알파벳 26개, 그림단어, 색깔 놀이, 화장실 화면, 음악, 스티커, 보호자 확인 화면 검사 통과 |
| 브라우저 JavaScript 오류 | 테스트 중 0건 |
| PWA 캐시 | Node 모의 캐시 테스트에서 선저장 파일 존재, 설치 완료 후 전환, 앱 캐시만 정리, 오프라인 HTML·연결 자산 반환 검사 통과 |

음성은 호출 순서와 안내 문구를 검사했다. 실제 iPad/iPhone Safari의 렌더링·음성 출력, 사용자 기기에 이미 설치된 PWA의 캐시 전환은 실기기에서 확인하지 않았다. PWA 모의 캐시 검사는 실제 브라우저 오프라인 설치 시험을 대신하지 않는다.

## 유지보수

- 엘리베이터 주행: js/v5.js
- 파노라마: js/v5-panorama.js
- 태블릿·휴대폰 레이아웃: css/v5-view.css
- 앱 진입 및 다른 놀이: js/app-v4.js
- 다음 배포 시 index.html의 자산 버전과 sw.js의 캐시 버전을 함께 변경한다.
